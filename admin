<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - MUA</title>
    <link rel="stylesheet" href="style.css">
    <style>
        body { background-color: #f4f4f4; color: #333; }
        .admin-container { max-width: 800px; margin: 50px auto; background: white; padding: 30px; border-radius: 8px; box-shadow: 0 4px 15px rgba(0,0,0,0.1); }
        .form-group { margin-bottom: 15px; }
        input, select { width: 100%; padding: 10px; margin-top: 5px; border: 1px solid #ddd; border-radius: 4px; }
        .admin-gallery { display: grid; grid-template-columns: repeat(auto-fill, minmax(100px, 1fr)); gap: 10px; margin-top: 20px; }
        .admin-img-card { position: relative; height: 100px; }
        .admin-img-card img { width: 100%; height: 100%; object-fit: cover; border-radius: 4px; }
        .delete-btn { position: absolute; top: 2px; right: 2px; background: red; color: white; border: none; cursor: pointer; padding: 2px 6px; font-size: 12px; border-radius: 4px; }
        .back-link { display: block; margin-bottom: 20px; color: #333; text-decoration: none; font-weight: bold; }
    </style>
</head>
<body>

    <div class="admin-container">
        <a href="index.html" class="back-link">← Retour au site</a>
        <h2>Panneau d'Administration</h2>
        
        <div class="add-photo-section">
            <h3>Ajouter une photo</h3>
            <div class="form-group">
                <label>URL de l'image (Lien direct)</label>
                <input type="text" id="imgUrl" placeholder="Ex: https://source.unsplash.com/..." required>
            </div>
            <div class="form-group">
                <label>Catégorie</label>
                <select id="imgCategory">
                    <option value="mariage">Mariage</option>
                    <option value="artistique">Artistique</option>
                    <option value="shooting">Shooting</option>
                </select>
            </div>
            <button onclick="addPhoto()" class="btn" style="width: 100%; background: #333; color: #d4af37;">Ajouter au Portfolio</button>
        </div>

        <hr style="margin: 30px 0; border: 0; border-top: 1px solid #eee;">

        <h3>Gérer les photos actuelles</h3>
        <div class="admin-gallery" id="admin-gallery">
            </div>
    </div>

    <script src="script.js"></script>
    <script>
        // Script spécifique à l'admin pour charger la vue admin
        document.addEventListener('DOMContentLoaded', () => {
            renderAdminGallery();
        });
    </script>
</body>
</html>